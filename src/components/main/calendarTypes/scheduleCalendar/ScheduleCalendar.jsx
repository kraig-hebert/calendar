import React from 'react';
import { useStyles } from './styles';

const ScheduleCalendar = () => {
  const classes = useStyles();
  return <div className={classes.scheduleCalendar}>ScheduleCalendar</div>;
};

export default ScheduleCalendar;

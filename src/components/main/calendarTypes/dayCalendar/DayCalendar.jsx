import React from 'react';
import { useStyles } from './styles';

const DayCalendar = () => {
  const classes = useStyles();
  return <div className={classes.dayCalendar}>DayCalendar</div>;
};

export default DayCalendar;

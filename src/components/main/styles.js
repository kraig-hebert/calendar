import { createUseStyles } from 'react-jss';

export const useStyles = createUseStyles({
  main: {
    width: '100%',
    padding: {
      top: '10px',
      left: '10px',
    },
  },
  hamburger: {
    fontSize: '1.5rem',
    color: '#000',
    cursor: 'pointer',
  },
});

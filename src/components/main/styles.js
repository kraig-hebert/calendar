import { createUseStyles } from 'react-jss';

export const useStyles = createUseStyles({
  main: {
    width: '100%',
    padding: {
      top: '10px',
      left: '10px',
    },
  },
  hamburger: {
    fontSize: '2rem',
    color: 'rgb(7,82,18)',
    cursor: 'pointer',
  },
});
